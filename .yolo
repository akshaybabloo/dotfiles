#!/bin/bash

current_dir=$(pwd)

# Source all files
for file in "$current_dir"/.{aliases,functions}; do
    echo "Sourcing $file..."
    if [ -r "$file" ] && [ -f "$file" ]; then
        source "$file"
    fi
done
unset file
